fastapi
uvicorn[standard]
gunicorn
python-dotenv
bcrypt
pyjwt
python-multipart
numpy<3.0.0

# Storage: SQLAlchemy Core abstracts SQLite (local dev) vs Postgres (Cloud SQL in production)
# behind the same db.py code. psycopg2-binary is only exercised when DATABASE_URL points at
# Postgres - harmless to install locally either way. Cloud Run connects to Cloud SQL over the
# built-in Unix socket (--add-cloudsql-instances), so no separate connector library is needed -
# see DEPLOY.md for the exact DATABASE_URL format that uses.
sqlalchemy>=2.0
psycopg2-binary

# Cloud Storage: backs up uploaded resumes + each user's Chroma vector DB so they survive a
# Cloud Run redeploy/cold start - only used when GCS_BUCKET_NAME is set (see gcs_storage.py).
google-cloud-storage

# LangChain core
langchain
langchain-community
langchain-core
langchain-text-splitters
zstandard>=0.24.0

# Vector store (same reasoning as the Streamlit app: langchain-chroma uses
# chromadb.PersistentClient() directly, avoiding the deprecated global-singleton
# chromadb.Client() tenant-bootstrap bug)
langchain-chroma==0.2.6
chromadb>=1.0.20,<2.0.0
# Linux x86_64 deployment only (no ARM/macOS wheels exist) - main.py should skip importing
# it gracefully via try/except if missing, same pattern as the Streamlit app.
pysqlite3-binary; sys_platform == "linux" and platform_machine == "x86_64"

# PDF / Word handling
pypdf
docx2txt
python-docx

# LLM providers
langchain-groq
langchain-openai

tiktoken

# Monetization - Career Sprint one-time purchase via Stripe Checkout (see routers/payments.py).
# Safe to install even with no Stripe keys set; the SDK just never gets a live key to call with.
stripe
